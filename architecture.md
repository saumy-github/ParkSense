# ParkSense Architecture

This document describes the current ParkSense prototype architecture using a Mermaid diagram. It highlights the frontend, backend, and ML/data components, plus the main API and file dependencies.

```mermaid
flowchart LR
    Browser[Web Browser] -->|HTTP requests| Frontend[React + Vite Frontend]
    Frontend -->|GET /api/hotspots| Backend[FastAPI Backend]
    Frontend -->|GET /api/hotspots/:cluster_id| Backend
    Frontend -->|GET /api/hotspots/:cluster_id/visual-summary| Backend
    Frontend -->|GET /api/hotspots/:cluster_id/frame| Backend

    subgraph BackendServices[Backend Data Services]
      Backend -->|reads JSON| HotspotSummary[ML/data/processed/hotspots_summary.json]
      Backend -->|reads JSON| HotspotDetails[ML/data/processed/hotspots_details.json]
      Backend -->|reads JSON| VisualSummary[ML/data/processed/hotspot_visual_summary.json]
      Backend -->|serves image| FrameFiles[ML/data/processed/hotspot_frames/*_annotated.jpg]
    end

    subgraph MLComponents[ML + Data Processing]
      RawData[ML/data/raw/parking/raw.csv]
      ProcessedData[ML/data/processed/]
      Notebooks[ML/notebooks/]
    end

    HotspotSummary -->|generated by notebooks| Notebooks
    HotspotDetails -->|generated by notebooks| Notebooks
    VisualSummary -->|generated by notebooks| Notebooks
    FrameFiles -->|generated by notebooks/visual pipeline| Notebooks

    subgraph Repository[Project Repository]
      Root[Repo Root]
      Docs[docs/]
      Agent[.agent/]
    end

    Root --> Frontend
    Root --> Backend
    Root --> MLComponents
    Root --> Docs
    Root --> Agent

    style Frontend fill:#1f2937,stroke:#60a5fa,stroke-width:2px,color:#dbeafe
    style Backend fill:#0f172a,stroke:#38bdf8,stroke-width:2px,color:#e0f2fe
    style MLComponents fill:#111827,stroke:#facc15,stroke-width:2px,color:#fef3c7
    style Repository fill:#111827,stroke:#34d399,stroke-width:2px,color:#d1fae5
    style Docs fill:#111827,stroke:#a78bfa,stroke-width:1px,color:#ede9fe
    style Agent fill:#111827,stroke:#f472b6,stroke-width:1px,color:#fce7f3
```

## Key components

- `website/frontend/`: React + Vite app that displays the ParkSense map, hotspot dashboard, and reporting UI.
- `website/backend/`: FastAPI backend exposing `/api/hotspots`, `/api/hotspots/{cluster_id}`, `/api/hotspots/{cluster_id}/visual-summary`, and `/api/hotspots/{cluster_id}/frame`.
- `ML/data/processed/`: Processed JSON files and annotated images consumed by the backend.
- `ML/notebooks/`: Notebook source for data cleaning, clustering, hotspot scoring, and visual evidence generation.

## Current architecture notes

- The backend is file-backed and does not require a separate database; it serves processed JSON and image assets directly from `ML/data/processed/`.
- The frontend fetches hotspot data from the backend and renders an interactive map/dashboard in the browser.
- The ML pipeline produces the data artifacts that power the API and the demo.
